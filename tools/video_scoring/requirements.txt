# video_scoring — video → MIDI pipeline runtime deps.
# Sized for a regular Python env (CPU). The Modal-side vision model
# (Moondream 2) is hosted in modal/modal_chatbot.py and accessed over HTTP, so
# it is NOT a dependency of this package.

mido>=1.3.0
numpy>=1.21.0
opencv-python>=4.5.0
scenedetect[opencv]>=0.6.3
requests>=2.28.0
# ffmpeg + ffprobe must be installed system-wide (apt install ffmpeg / brew install ffmpeg).
# This package shells out to them for frame extraction and duration probing.
