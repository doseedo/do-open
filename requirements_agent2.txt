# Agent 2: Rich Feature Extraction Specialist
# Dependencies for 600D multitrack feature extraction

# Core scientific computing
numpy>=1.21.0
scipy>=1.7.0

# MIDI processing
pretty_midi>=0.2.9
mido>=1.2.10

# Progress bars and utilities
tqdm>=4.62.0

# Optional: For base feature extractor support
# (Already in main requirements.txt if available)
